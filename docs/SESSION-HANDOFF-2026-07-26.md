# Handoff — סשן 26.7.2026
## מה נעשה היום, ומה פתוח

> נכתב לקראת הרצת `/seo-audit` בסשן חדש. קרא את זה קודם כדי לא לחזור על עבודה שכבר בוצעה.

---

## מסמכים שנוצרו היום (קרא לפי סדר)

1. **`SEO-ACTION-PLAN-2026-07-26.md`** — תוכנית ראשית מבוססת נתוני GSC אמיתיים
2. **`PROMOTION-PLAN-loans-business-2026-07-26.md`** — תוכנית ממוקדת להלוואות + אשכול עסקים
3. `SEO-MASTER-AUDIT-2026-07.md` (קיים מ-1.7) — עדיין רלוונטי חלקית, ראה "מה השתנה" למטה

---

## קומיטים שנדחפו לפרודקשן היום

| Commit | תוכן |
|--------|------|
| `454b00e` | התקנת Google Analytics 4 (`G-LQGTH0XT3M`) דרך `next/script` ב-root layout |
| `2accf1b` | אופטימיזציית 7 דפים לפי ביקוש GSC אמיתי |
| `2d2d4d9` | אופטימיזציית 9 דפים נוספים (כותרות עם מספרים קונקרטיים) |
| `e512ff1` | קישוריות פנימית לאשכול `/business` + קישורי מימון דו-כיווניים |

**16 דפים אופטמו סה"כ.** כל מספר בכותרות נשלף מקבועי המחשבונים ב-`lib/calculators/` — לא הומצא. tsc נקי, 1707/1707 בדיקות עוברות.

---

## ממצאי המפתח מנתוני GSC (27.4–24.7.2026)

**האתר בן ~9 שבועות באינדקס** (חשיפה ראשונה 21.5.2026). 90 יום: ~148 קליקים, ~25K חשיפות, מיקום ממוצע ירד מ-50 ל-20.

### 1. אבחנת המסמך האב כבר לא נכונה
`SEO-MASTER-AUDIT-2026-07.md` קבע ש-"Crawled – not indexed" הוא החסם המרכזי. **זה נפתר** — URL Inspection מחזיר "Submitted and indexed" לכל הדפים שנבדקו, ו-100+ דפים צוברים חשיפות.

### 2. הצוואר בקבוק החדש: מיקום, לא אינדוקס
מתוך 500 מילות מפתח: רק **7** בטווח 11-20. **410 במיקום 21+**. חתימה של אתר שגוגל מכיר אך לא סומך עליו → סמכות off-site היא התקרה האמיתית.

### 3. ⚠️ אשכול `/business` לא קיים עבור גוגל
`/business/cafe` → **"URL is unknown to Google"**. האשכול (20 דפים) עלה 11.7, אבל גוגל הוריד את ה-sitemap לאחרונה **4.6**. טופל חלקית היום (קישורים פנימיים + IndexNow), אבל **ההגשה הידנית ב-GSC עדיין לא בוצעה** — ראה "פתוח" למטה.

### 4. חלון עונתי: מענק עבודה 🔴
נפח "מענק עבודה" עלה פי 6.7 מינואר (70→471 שבועי ב-Bing). דף `work-grant` צבר 2,025 מתוך 2,267 חשיפות רבעוניות **בחודש האחרון**. חלון ההגשה: **אוגוסט–ספטמבר**.
כוונת **"בדיקת זכאות"** (208 חשיפות בווריאציות) יושבת במיקום ~34 — **דף ייעודי טרם נבנה**.

### 5. אזהרה: אל תפרש שאילתות AI fan-out כבעיית CTR
`/blog/vat-complete-guide-israel` מציג 1,159 חשיפות במיקום 7.4 עם 0 קליקים — נראה ככשל CTR, אבל רוב החשיפות הן שאילתות מכונה של גוגל ("ישראל שיעור מע"מ 2026 18% רשות המסים" — 117 חשיפות). זה **סיגנל חיובי** (ה-AI מצטט אתכם), לא תקלה.

---

## פתוח — לפי עדיפות

### ⛔ חוסם, ורק המשתמש יכול לבצע (5 דקות ב-GSC)
1. **הגשה מחדש של `sitemap.xml`** — גוגל לא קרא אותו מ-4.6
2. **Request Indexing** ל-`/business`, `/business/cafe`, `/business/barbershop`, `/business/gym`, `/business/online-store`

*(IndexNow כבר נשלח היום ל-21 URLs — Bing 200, Yandex 202. גוגל לא תומך ב-IndexNow.)*

### 🔴 רגיש לזמן
3. **דף `/employee-rights/work-grant/eligibility`** — flow זכאות כן/לא + `HowTo`/`FAQPage` schema. החלון נסגר בספטמבר.
4. דף מענק עבודה רטרואקטיבי (2020-2025) — ה-FAQ כבר מזכיר 6 שנים אחורה, ויש חיפושים בפועל

### 🟡 טווח בינוני
5. 10 דפי `/personal-tax/salary-net-gross/[amount]` — **חובה חישוב מלא ייחודי לכל דף**, אחרת זה doorway pages
6. העשרת 5 דפי hub (מיקום 70-90): `/personal-tax`, `/investments`, `/real-estate`, `/loans`, `/employee-rights`
7. מחשבון `/tools/vat-extract` ("חילוץ מעמ" — כוונה נפרדת מהמדריך הכללי)

### 🟢 מתמשך — התקרה האמיתית
8. סמכות off-site (OF-1..OF-6 במסמך האב) — בלי זה יש תקרה שאופטימיזציה on-page לא תפרוץ
9. ⛔ **חסום מ-1.7:** מספר רישיון רו"ח + LinkedIn ל-`sameAs`. תנאי-סף ל-E-E-A-T באתר YMYL.

---

## הערות טכניות למי שממשיך

- **מקור אמת יחיד למספרים:** `lib/calculators/*.ts` ו-`lib/constants/tax-2026.ts`. אתר YMYL — נתון שגוי אחד פוגע באמון של גוגל ושל המשתמשים. אל תכתוב מספר לכותרת בלי לאמת בקוד.
- טבלת ברוטו→נטו ב-`salary-net-gross/page.tsx` **מחושבת בזמן build** מ-`calculateSalaryNetGross` — עקבית עם המחשבון by construction. אל תחליף במספרים קשיחים.
- `BUSINESS_TYPES` ב-`lib/data/business-setup/business-types.ts` מערבב `slug: '...'` ו-`"slug": "..."` — grep עם גרשיים בודדים מחזיר 2 מתוך 20.
- דפים שכבר מתפקדים ולא נגעתי בהם בכוונה: `hourly-rate` (CTR 5%), `sick-pay` (2.6%), `annual-leave`, `fuel-cost`.

---

*נוצר 26.7.2026 · מבוסס GSC API, URL Inspection, Bing Keyword Research*
